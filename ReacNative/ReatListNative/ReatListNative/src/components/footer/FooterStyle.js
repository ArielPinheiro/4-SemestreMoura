import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 16,
    alignItems: "center",
  },
  text: {
    color: "#4a9a4a",
    fontFamily: "monospace",
    fontSize: 12,
  },
});